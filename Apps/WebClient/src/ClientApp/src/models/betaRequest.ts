export default interface BetaRequest {
    // The user id for the request.
    hdid: string;
    // The email for the request.
    emailAddress: string;
    // The version of the record.
    version: number;
}
