export default interface ErrorCard {
    // The title of the error message
    title: string;

    // The descriptive text below the title
    description: string;

    // The error code
    code: string;
}
