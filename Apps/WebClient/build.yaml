# https://aka.ms/yaml
name: 1.0.0$(Rev:.r) #Build number

trigger:
 batch: 'true'
 branches:
   include:
     - dev
 paths:
   include:
     - Apps/WebClient
 
pool:
  name: 'HealthGateway'

steps:
- script: echo Hello, world2!
  displayName: 'testing a 1 line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml 
    echo $(Build.BuildNumber)
    oc projects
  displayName: 'Testing multi-line scripts'

