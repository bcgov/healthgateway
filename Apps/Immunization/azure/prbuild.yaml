name: 1.1.0$(Rev:.r) #Build number

pr:
    branches:
        include:
        - dev
    paths:
        include:
        - Apps/Immunization
        - Apps/Common
        - Apps/Database

pool:
    name: "HealthGateway"

extends:
  template: /Build/hg.yaml
  parameters:
    Application: Immunization
    DotNetTests: true
    FunctionalTests: false
    NPMInstall: false
    NPMTests: false

