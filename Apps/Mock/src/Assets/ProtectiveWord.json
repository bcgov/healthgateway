{
    "uuid": "${uuid}",
    "hdid": "${hdid}",
    "requestingIP": "${requestingIP}",
    "maintainProtectiveWord": {
        "operator": "Get",
        "PHN": "${phn}",
        "value":"${value}"
    }
}
