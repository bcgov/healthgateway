name: PR$(Rev:.r) #Build number

pr:
  branches:
    include:
      - dev
  paths:
    include:
      - Apps/PatientDataAccess

trigger: none

extends:
  template: /Build/hg.yaml
  parameters:
    Application: PatientDataAccess
    DotNetTests: true
    NPMInstall: false
    Library: true
